/**
 * 
 */
package ch.ethz.matsim.supernetwork.clustering.cluster.utils;

import java.util.ArrayList;
import java.util.List;

import ch.ethz.matsim.supernetwork.clustering.cluster.Cluster;
import ch.ethz.matsim.supernetwork.clustering.element.ElementActivity;

/**
 * @author stefanopenazzi
 *
 */
public class Sorter {
	
	public static void sortClusterComponentsByCentroidDist(Cluster<ElementActivity> cluster) {
		/*
		 * List<ElementActivity> newSortedList = new ArrayList(); double xCentroid =
		 * cluster.getCentroid().getX(); double yCentroid =
		 * cluster.getCentroid().getY(); for(ElementActivity i: cluster.getComponents())
		 * { double dist = Math.pow(xCentroid - i.getNextActivity().getCoord().getX(),
		 * 2)+ Math.pow(yCentroid - i.getNextActivity().getCoord().getY(), 2);
		 * for(ElementActivity j: newSortedList ) {
		 * 
		 * } }
		 */
	}

}
